# Clean Hero Update

Replace these two files in the local `Florida-Liquor-License-Market` repository while the `development` branch is selected:

- `components/HomePage.tsx`
- `app/globals.css`

This update:
- removes the screenshot-derived hero backgrounds,
- eliminates ghosted duplicate text and controls,
- moves featured listings outside the hero,
- keeps the search filters functional,
- preserves the black-and-gold marketplace design,
- remains responsive.

Commit message:
`Rebuild hero with clean responsive artwork`
